# Dependencias
node_modules/

# Salida de build
dist/

# Cache de Astro
.astro/

# Salida del adaptador de Vercel (build del panel de edición)
.vercel/

# Variables de entorno (NUNCA subir claves al repo)
.env
.env.production
.env.*.local

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Sistema operativo / editores
.DS_Store
Thumbs.db
.vscode/*
!.vscode/extensions.json

# Material fuente de fotos y vídeos (NO va al repo; a la web solo van public/uploads)
MATERIAL FOTOS RRSS/
MATERIAL VIDEOS RRSS/
COCHES Videos y fotos/

# Operativa de redes sociales (sistema + assets pesados + node_modules propios).
# NO va al repo de la web ni a su despliegue. Es un paquete aparte que vive en la
# carpeta pero no afecta a calmasociety.com. Ver REDES-SOCIALES/README.md.
REDES-SOCIALES/
.tmp_research/

# Compañero visual de brainstorming (maquetas temporales; no van al repo)
.superpowers/

# Secretos locales de wrangler dev (nunca al repo)
.dev.vars

# Estado local de wrangler dev (D1 local, cache)
.wrangler/

# Negocio y estrategia (contactos de empresas, tratos, dinero): NUNCA a un repo
# público. Su copia de seguridad es el script de respaldo (KAN-53), no git.
COLABORACIONES/
NEGOCIO/
ESTRATEGIA/
_PANEL/

# Herramientas del agente y ficheros de trabajo temporales. No son código de la
# web: se regeneran solos y ensucian el historial (un `git add -A` se llevó 1.235
# ficheros de aquí el 24-ago-2026 dentro de un commit de i18n).
.claude/
.agents/
.codex/
.impeccable/
.superpowers/
skills-lock.json
.github/hooks/
.github/skills/
.tmp_wiki/
.tmp_cruise/
.tmp_i18n/
*.tgz

# Identidad de marca (logos, plantillas, fuentes, pines): assets pesados que no
# sirve a la web. Mismo criterio que ESTRATEGIA/ y NEGOCIO/ de arriba — su copia
# de seguridad es el script de respaldo (KAN-53), no git.
marca/
MARCA-PARA-ABRIR/
